const [START, TEST, MAIN, REVIEW, RESULT, DETAIL, LOADING] = ["start", "test", "main", "review", "result", "detail", "loading"];
export {
    START,
    TEST,
    MAIN,
    REVIEW,
    RESULT,
    DETAIL,
    LOADING
};
